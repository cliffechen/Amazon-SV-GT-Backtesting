# 首批运行记录

报告日期：2026-09-23（北京时间）。Amazon 数据截止周：2026-09-12。

## 数据与预算

原始资料是根目录 `ingredient_db.json`：22类、184条记录、163种原始名称，清洗后151个候选条目。它是候选知识库，不是历史趋势数据库；原文件SHA256保持不变：`2decbc7bf34c1154a2ff33980fb74407d02123f64f4a9f5d6b16fb60bae2b256`。

首批选择40个查询映射，共9409行成分周记录；39个有241周历史，绿茶提取物仅10周，不输出数值预测。新增78次核心历史请求，复用2份已有缓存，再用1次批量关键词请求取得40词的竞争快照。本轮共79次SellerSprite调用，账本无待完成预约。项目120次上限余41；用户截图余额1184扣除本项目调用后估计1105，并非实时余额。Sorftime本轮无新增调用。

## 回测结果

早期验证负责选模，后期只评估预设策略；另按成分族留组。WAPE是总绝对误差除以总真实搜索量，越低越好，不能用100%减去它称为准确率。

| 指标 | 13周 | 26周 |
|---|---:|---:|
| 早期选择 | 最近13周均值 | 去年同期均值 |
| 所选模型后期WAPE | 33.20% | 50.16% |
| 同期近期均值基准WAPE | 33.20% | 34.53% |
| 所选模型留组后期WAPE | 20.33% | 27.99% |
| 留组近期均值基准WAPE | 20.33% | 21.88% |
| 所选模型后期经验区间覆盖率 | 76.18% | 56.30% |

本批尚无稳定预测增益证据。尿石素A的26周季节性外推虽然约53074次/周、相对近期均值增长161.8%，不能据此声称即将火爆。Google信号在部分时间跨度改善、其他跨度或留组成分未改善；ABA排名也未证明稳定额外收益。没有依据已查看的后期成绩回头挑选模型。

独立审计确认训练标签在预测起点前成熟、历史训练排除留组成分、标准化只拟合训练数据、12组经验区间分位数仅来自早期验证；输入和建模代码摘要与训练清单一致。当前下载的历史可能修订，仍不等于真实历史当时可获取的数据；需继续保存未来快照验证。

## 报告与新闻

`outputs/latest/report.html` 内嵌图表程序和数据，含成分切换、13/26周切换、可拖动时间条、Google联动、ABA开关、历史预测对比、竞争快照、近三日历月新闻和预算。新闻目前重点覆盖尿石素A/Timeline，并补充肌酸与南非醉茄；首批均涉及美国，加拿大与墨西哥未完成系统检索，其他成分空白不代表无新闻。品牌推广与媒体来源有明确标签。

竞争数据为2026.08月份快照，不冒充完整周历史或真实广告扣费；PPC、集中度、TikTok及新闻未进入首版训练。

## 验收

- Python全套测试：62项通过（16.70秒），覆盖预算并发与拒绝、缓存身份/周期、未来数据边界、模型、新闻、报告渲染和无效成分选择。
- 浏览器以本地HTTP预览验证：搜索/切换成分、13/26周切换、实际拖动时间条与双图联动、ABA曲线开关、Timeline品牌/媒体筛选、短历史显示数据不足；无浏览器错误日志、无横向溢出。
- 浏览器安全策略不允许直接访问file URL，因此未在内置浏览器验证直接文件打开；不影响已有HTTP交互验证。单文件是否依赖外部资源由生成代码和测试检查。
- Skill格式验证通过；项目内Skill与 `C:/Users/Nexus/.codex/skills/ingredient-forecast/SKILL.md` 内容一致。独立缓存工作流冒烟：尿石素A计划0新调用、2缓存命中。

使用入口是Codex对话中的 `$ingredient-forecast`。Python执行数据对齐和计算，Skill组织调用与解释，HTML用于查看；离线重算不会刷新MCP数据或搜索新闻。
